---
name: laowang-xiaohongshu-positioning
description: 通过一对一渐进访谈，帮助第一次做小红书的创作者从零完成个人定位、账号定位、内容定位和账号包装，并在用户逐阶段确认后创建或更新 Obsidian 定位文件。用于用户开始或继续小红书定位访谈、回答或修改个人定位、账号方向和内容定位、生成昵称简介头像背景图方案，或将已确认结果写入定位总览和账号包装文件时。
---

# 老王内容定位——小红书版

## 目标

引导用户形成一版可直接使用、允许后续迭代的小红书账号定位。平台固定为小红书，但不预设账号赛道或内容方向；根据用户的真实经历、能力、兴趣、资源、目标读者和长期意愿梳理方向。

## 启动

1. 检查当前工作区是否为用户的 Obsidian Vault，并确认存在 `00_个人背景/`。不要在磁盘中广泛搜索其他目录。
2. 如果目录缺失，说明缺少 `00_个人背景/` 并询问用户是否创建。用户明确同意后创建目录并继续；用户拒绝或未明确同意时暂停。
3. 告知用户将依次完成个人定位、账号定位、内容定位和账号包装。
4. 在同一条消息末尾提出个人定位的第一个问题，然后等待。不要要求用户发送额外启动口令。

## 对话规则

- 一次只问一个主要问题。
- 根据上一条回答继续追问，不照搬固定问卷。
- 优先询问真实经历、具体时间、实际动作、结果、失败和修正过程。
- 用户想不起来时，提供 2—3 个回忆方向；不要替用户编造答案。
- 明确区分用户事实、用户偏好、AI 建议和待确认信息。
- 不使用用户没有说过的身份、能力、成绩、性格标签或商业结果。
- 不主动搜索小红书或互联网验证市场需求、差异化或商业价值。
- 不判断潜在商业价值或变现前景。
- 不要求用户记住或发送流程指令。
- 信息足够时主动停止提问，不为凑数量继续访谈。
- 每个阶段先生成结果并主动请求确认；用户确认后自动进入下一阶段。
- 用户指出错误时，只修改相关内容，保留其他已确认信息，再次请求确认。
- 只有用户明确确认的内容才能写入正式字段；其余必要内容标记为“待确认”。
- 询问公开边界，但不主动索取不必要的敏感细节。只记录不可公开信息的类别或边界，不复述或写入具体敏感内容。

需要提问、追问或判断信息是否充足时，读取 [references/interview-guide.md](references/interview-guide.md)。

## 阶段一：个人定位

收集并整理：

- 当前身份和阶段；
- 与工作、学习、生活、项目或内容创作有关的真实经历；
- 做成、帮助别人或踩坑过的具体事情；
- 已有能力及事实依据；
- 正在学习和实践的方向；
- 熟悉、愿意长期关注或拥有资源的领域；
- 适合采用的内容视角；
- 可以公开、需要匿名和不可公开的信息边界。

生成“个人定位 v0.1”，区分已确认事实、用户偏好、AI 建议和待确认信息。主动询问是否准确。确认后保留阶段结果并自动进入账号定位。

## 阶段二：账号定位

围绕已确认的个人定位继续确认：

- 用户愿意长期关注和表达的主题；
- 用户有事实依据的经验、能力、行动或学习方向；
- 适合的表达方式和深度；
- 目标读者当前的认知状态和真实问题；
- 读者关注后可以获得的价值；
- 用户真实经历带来的视角；
- 当前可持续投入的时间和执行条件。

信息足够后生成 2—4 个有事实依据的候选账号方向。不要为凑数量生成牵强方向；不足两个可靠方向时继续访谈。

对每个候选方向进行定性比较：

1. 个人事实与能力匹配度；
2. 长期创作可持续性；
3. 目标读者需求；
4. 差异化；
5. 执行难度。

只使用事实依据、优势、不足或风险和执行要求进行说明，不使用星级、分数、百分比或其他量化排名。目标读者需求和差异化若仅来自用户描述或 AI 推断，标记为“待验证”，不得声称已经完成市场调研。

推荐其中一个候选方向并说明依据，但由用户比较后选择。候选方向只用于当前对话；最终文件只保留用户选择并确认的方向。

用户选择后，使用主题、表达方式、读者认知状态、核心价值和个人视角生成一句话账号定位与结构化账号定位，并主动询问是否准确、自然且愿意持续使用。确认后自动进入内容定位。

## 阶段三：内容定位

结合前两阶段生成：

- 一个核心内容主题；
- 2—3 个相互一致的内容支柱；
- 每个支柱解决的目标读者问题；
- 与所选方向匹配的内容形式，不预设固定类别或数量；
- 前 5 条可执行的候选选题；
- 后续可扩展方向；
- 暂时不做的内容及真实性或事实核验原则。

前 5 条选题应同时满足：对应真实读者问题、符合用户当前能力和经验、范围具体、有可靠资料或真实经历支撑、能体现账号方向、不把推测包装成事实。

只有用户确认选择 AI 方向时，才可以按需参考 AI 科普、AI 热点、AI 测评和 AI 教程；不得强制全部采用。不要把未真实使用的工具写成测评，也不要把未完整跑通的流程写成教程。

生成“内容定位 v0.1”并主动请求确认。确认后自动进入账号包装。

## 阶段四：账号包装

生成账号包装前，读取 [references/profile-bio-guide.md](references/profile-bio-guide.md)。先复用前三阶段的已确认信息，判断账号主体、目标读者是否有明确地域限制、地域是否影响读者选择、用户的露脸意愿、可公开的证明信息，以及搜索识别与个人品牌记忆的相对重要性；不要展示新的固定问卷。只有缺失信息会直接改变包装方案时，才按照原有对话规则补问当前最关键的一题。

按指南中的适用条件选择昵称、头像和背景图方法，再识别用户最有价值的“简介锚点”。不要因为用户居住在某地就把账号判断为本地业务，不要为了套用方法添加无关地区、身份、数据或效果承诺，也不要把内部账号定位公式直接改写成主页简介。

基于已确认定位生成 2—3 套彼此有真实差异的文字方案。只有两套成立时不补第三套。可从以下角度组合：

- 主题价值型：直接说明讲什么、怎样讲、关注后获得什么；
- 个人视角型：突出用户的真实身份或经验视角；
- 成长记录型：适合仍在学习和实践中的创作者。

每套方案包含：

- 头像方向；
- 昵称；
- 简介锚点；
- 2—4 行自然、简短且有辨识度的简介；
- 背景图文案；
- 适用理由和潜在风险。

方案必须采用与其类型匹配的信息结构，不能只替换同一模板中的几个词。只推荐其中一套并说明依据，由用户最终选择。检查昵称是否自然、简介是否真实，陌生人能否快速知道为什么值得听、会持续看到什么、为什么值得关注，并至少记住一个真实细节。不要编造头衔、经历、数据或成果。

昵称方法只是条件式参考，不要求每种方法都生成，也不限制其他自然且符合定位的名称。行业关键词只能说明可能提高主题识别或搜索匹配，不能承诺搜索排名、流量、关注或成交。头像选择必须服从用户的露脸意愿、匿名要求和公开边界；背景图只承担一个主要信息任务，避免与昵称和简介重复堆叠。

只输出头像和背景图的文字方向，不生成图片文件。主动询问用户是否采用或需要修改。最终方案确认后，立即进入自动写入，不等待额外指令。

## 自动写入 Obsidian

只在四个阶段全部完成且最终账号包装确认后写入，不创建阶段性草稿或恢复文件。

1. 读取 [references/output-templates.md](references/output-templates.md)。
2. 创建或更新：
   - `00_个人背景/定位总览_v0.1.md`
   - `00_个人背景/账号包装_v0.1.md`
3. 只写入用户明确确认的信息；账号定位只保留用户最终确认的方向。
4. 将尚未确认但必须保留的判断写为“待确认”，不要擅自补全。
5. 只记录需要匿名或不可公开的信息类别和边界，不写入具体敏感内容。
6. 文件已存在时先读取，再更新对应章节；保留用户手动添加的无关内容，不做整文件盲目覆盖。
7. 写入后重新读取两个文件，检查路径、标题、关键字段和待确认项。
8. 向用户报告已生成或更新的文件，不再要求用户发送写入命令。

## 完成标准

只有同时满足以下条件才结束：

- 四个阶段均已由用户确认；
- 个人经历和能力有事实依据，敏感信息只记录边界；
- 用户已从 2—4 个可靠候选方向中选择并确认最终账号方向；
- 账号主题、表达方式、读者认知状态和核心价值一致；
- 2—3 个内容支柱承接账号定位，前 5 条候选选题可执行；
- 账号包装与定位一致且没有虚构信息；
- 两个 Obsidian 文件已成功写入并复查。
